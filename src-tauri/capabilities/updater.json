{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "updater",
  "description": "auto-updater permissions scoped to the panel window (where the settings AboutPage lives)",
  "windows": [
    "panel"
  ],
  "permissions": [
    "updater:default",
    "process:default"
  ]
}
